@using AnimalStudio.Web.ViewModels
@model IEnumerable<AnimalStudio.Web.ViewModels.Procedure.ProcedureIndexViewModel>

@{
	ViewData["Title"] = "Procedures List!";
}
<h1>@ViewData["Title"]</h1>


@* <div class="row">

	@foreach (var procedure in Model)
	{
		<div class="card" style="width: 10rem; margin: 2rem">
			<div class="card-body">
				<h5 class="card-title">@procedure.Name</h5>
				<p class="card-text">@procedure.Price EUR</p>
				<a href="ProcedureDetails" class="btn btn-primary">Details</a>
				<a href="DeleteProcedure" class="btn btn-primary">Delete</a>
			</div>
		</div>
	}
</div>

<table>

	<tr>
		<th class="text-center">Id</th>
		<th class="text-center">Name</th>
		<th class=" text-center">Price</th>
		<th>WorkerId</th>
	</tr>

	@foreach (var procedureView in Model)
	{
		<tr>
			<td>@procedureView.Id</td>
			<td>@procedureView.Name</td>
			<td>@procedureView.Price</td>
			<td class="text-center">@procedureView.WorkerId</td>
			<td><a href="AddProcedure" class="btn btn-primary">Add</a></td>

		</tr>

	}
</table>
 *@



<div class="d-flex justify-content-between align-items-center mb-3" style="margin-top: 20px;">
    <h2>@ViewData["Title"]</h2>
    @if (User?.Identity?.IsAuthenticated == true)
    {
        <a asp-controller="Procedure" asp-action="AddProcedure" class="btn btn-primary"><i class="fas fa-plus"></i>Add Procedure</a>
    }
</div>
<hr />

<div class="row text-center">
	@foreach (var procedure in Model)
	{
		<div class="col-md-3 mb-4">
			<div class="card h-100" style="width: 100%;">

				<div class="card-body d-flex flex-column">
					<h5 class="card-title">@procedure.Name</h5>
					<p class="card-text">Price: @procedure.Price.ToString("C", System.Globalization.CultureInfo.CreateSpecificCulture("fr-FR"))</p>
					<div class="d-flex justify-content-between mt-auto">
@* 						@if (User?.Identity?.IsAuthenticated == true)
						{
							<a asp-controller="Procedure" asp-action="ProcedureDetails" asp-route-id="@procedure.Id" class="btn btn-info me-2 w-50">Details</a>
							<form asp-controller="Product" asp-action="AddToCart" method="post" class="w-50">
								<input type="hidden" name="id" value="@procedure.Id" />
								<button type="submit" class="btn btn-success w-100">Buy</button>
							</form>
						}
						else
						{
 *@							<a asp-controller="Procedure" asp-action="ProcedureDetails" asp-route-id="@procedure.Id" class="btn btn-info w-100">Details</a>
						@* } *@
					</div>
				</div>
			</div>
		</div>
	}
</div>

@section Scripts {
	@{
		await Html.RenderPartialAsync("_ValidationScriptsPartial");
	}
}
