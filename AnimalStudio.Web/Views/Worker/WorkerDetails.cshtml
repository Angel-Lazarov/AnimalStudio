@using AnimalStudio.Web.ViewModels.Worker
@model WorkerViewModel


@{
	ViewData["Title"] = "Worker Details!";
}

<div class="d-flex justify-content-between align-items-center mb-3" style="margin-top: 20px;">
	<h2>@ViewData["Title"]</h2>
</div>
<hr />

<div>
	<h5 class="text-center">Details for worker: @Model.Name</h5>
</div>
<hr />



@* <div class="row justify-content-center" style="margin: 0 auto; max-width: 800px;">
    <div class="col-md-8">

            <h5 class="text-center">Worker Name: @Model.Name</h5>
            <hr />
            <h6 class="text">Procedures by worker</h6>
            @foreach (var procedure in Model.Procedures)
            {
	            <p class="text">
                @procedure.Name
                @procedure.Price
              </p>
            }

        <div class="card-footer text-center">
            <div class="d-flex justify-content-between align-items-center">
                <a asp-controller="Worker" asp-action="Index" class="btn btn-warning d-flex justify-content-center align-items-center flex-fill mx-1">Back</a>
                <a asp-controller="Worker" asp-action="DeleteWorker" asp-route-id="@Model.Id" class="btn btn-danger d-flex justify-content-center align-items-center flex-fill mx-1" onclick="return confirm('Are you sure you want to delete this worker?');">Delete</a>
            </div>
        </div>
    </div>
</div> *@

@if (User?.Identity?.IsAuthenticated ?? false)
{
	<div class="text"><strong>Worker Management</strong> </div>
	<a asp-controller="Worker" asp-action="EditWorker" asp-route-id="@Model.Id" class="btn btn-info d-inline">Edit</a>
	<a asp-controller="Worker" asp-action="DeleteWorker" asp-route-id="@Model.Id" class="btn btn-danger d-inline" onclick="return confirm('Are you sure you want to delete this worker?');">Delete</a>


/*
	<div>
		<div class="text"><strong>Worker Management</strong> </div>

		<form asp-controller="Worker" asp-action="EditWorker" asp-route-id="@Model.Id" method="post" class="d-inline">
			@* <a asp-controller="Worker" asp-action="EditWorker" asp-route-id="@worker.Id" class="btn btn-info d-flex justify-content-center align-items-center flex-fill mx-1">Edit</a> *@
			<input name="Name" value="@Model.Name"/>
			<button type="submit" class="btn btn-warning justify-content-center align-items-center flex-fill mx-1">Edit</button>
			<a asp-controller="Worker" asp-action="DeleteWorker" asp-route-id="@Model.Id" class="btn btn-danger d-inline" onclick="return confirm('Are you sure you want to delete this worker?');">Delete</a>
		</form> 

	</div> */

}
<hr />
<hr />
<div class="text-center"><strong>Procedures for the worker</strong> </div>
<table class="table table-striped">
	<thead>
		<tr>
			<th>Name</th>
			<th>Price</th>
			<th>Actions</th>
		</tr>
	</thead>
	<tbody>
		@foreach (var procedure in Model.Procedures)
		{
			<tr>
				<td>@procedure.Name</td>
				<td>@procedure.Price</td>
				<td>
					@* <a asp-controller="Procedure" asp-action="AddProcedure" class="btn btn-info justify-content-center align-items-center flex-fill mx-1">Add Procedure</a> *@		
					<a asp-controller="Procedure" asp-action="EditProcedure" asp-route-id="@procedure.Id" class="btn btn-info justify-content-center align-items-center flex-fill mx-1">Edit Procedure</a>
				</td>
			</tr>
		}
	</tbody>
</table>